# Production API URL (set in Render frontend env vars before build).
# Leave unset locally to use the Vite dev proxy at /api → http://localhost:3001
# VITE_API_BASE_URL=https://assessment-wx9c.onrender.com
